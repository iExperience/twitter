<%= render partial: "header" %>

<h4>Tweets</h4>

<%= render :partial => 'errors', :locals => {:tweet => @tweet} %>

<br/><br/>
<%= render :partial => 'form', :locals => {:tweet => @tweet} %>
<br/>

<ul>
<% @tweets.each do |tweet| %>
  <li><%= link_to(tweet.user.handle, "/users/#{tweet.user.id}") %>: <%= tweet.body %></li>
<% end %>
</ol>